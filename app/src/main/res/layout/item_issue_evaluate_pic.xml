<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="showDel"
            type="java.lang.Boolean" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.yunshang.haile_life.ui.view.RoundImageView
            android:id="@+id/iv_issue_evaluate_pic"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:background="@color/transparent"
            android:src="@mipmap/icon_fault_add_pic"
            app:radius="8dp" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_issue_evaluate_pic_del"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:backgroundTint="@color/transparent"
            android:src="@mipmap/icon_fault_repairs_pic_del"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:visibility="@{showDel}" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>